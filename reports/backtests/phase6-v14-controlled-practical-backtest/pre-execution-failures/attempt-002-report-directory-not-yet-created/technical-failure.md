# V14 technical rerun evidence

This D1/Normal/native run used the final zero-warning harness and the final trading configuration, but MT5 could not emit its HTML report because the relative terminal report directory did not yet exist. The tester statistics, cache, logs, runtime evidence and isolated state are preserved unchanged.

The result is excluded as incomplete evidence. The permitted replacement run uses the byte-identical EA and strategy configuration after creating only the non-trading report directory. The observed losing result was not a reason for the rerun and is not discarded.

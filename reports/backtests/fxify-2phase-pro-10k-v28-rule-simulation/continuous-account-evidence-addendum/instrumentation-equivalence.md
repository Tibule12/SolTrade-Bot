# Instrumentation equivalence

`PASS` — maximum unexplained divergence is zero. The zero-delay reference/instrumented pair and 200 ms reference/instrumented pair have byte-identical deals, transactions and events. High and Stress also have byte-identical native streams to Normal; their frozen 0.5x/1.0x supplementary-friction layers are reporting adjustments and are not described as broker-native fills. The instrumentation observes account state only.

# Native run directory

No native MT5 report or log exists because the V11 pre-run evidence audit failed before run 1. All 18 planned statuses are recorded as `NOT_RUN_PRECHECK_FAILED` in the run inventory. Empty or fabricated tester reports are deliberately not presented as run evidence.
